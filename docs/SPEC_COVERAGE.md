# Product Specification Coverage Ledger

This ledger keeps implementation claims tied to the LocalView AI-Native Localhost Runtime Product Spec. `Implemented` means the capability is connected through the live product path and has verification evidence. `Partial` means useful code exists but one or more required runtime integrations are still missing. `Planned` means the product capability is still specification-only or has only non-executing scaffolding.

| Spec capability | Status | Repository surface / next gate |
| --- | --- | --- |
| Native Rust core | Implemented | `crates/core`, daemon, CLI |
| Listener discovery | Implemented | `crates/discovery` |
| Frontend HTTP classification | Implemented | Vite/Next/Nuxt/Svelte/Astro/Angular/Webpack/Storybook evidence |
| Port-independent project identity | Implemented | `localview-core` + sessions |
| Session lifecycle / reconnect grace | Implemented | `crates/sessions` |
| Tray + close-to-tray desktop shell | Implemented | Tauri desktop |
| Lightweight native preview | Partial | Native WebView preview and feature-gated child workspace exist; iframe remains the conservative default until cross-platform composition/focus/DPI policy is validated. V4.3 W10 physical mixed-DPI closure remains draft on PR #116 and requires self-hosted multi-monitor evidence; guarded full-page closure does not satisfy that hardware gate. This hardware-only proof is deferred from the current software critical path and does not block the remaining Wave 4–9/source-runtime work |
| Human View zero-clutter workspace | Partial | Full-canvas workspace and floating chrome exist; native child-WebView policy gate remains |
| X-Ray / Inspector UI | Implemented | Direct Human Inspector point-select is wired to the exact LocalView-managed WebView with bounded hover highlight, one-shot click suppression/cleanup, Escape cancellation, route/session/document-generation fencing and stable `ElementRef` authority. The selected ref feeds the existing Measure, Open Source, Ask AI, Fix and Verify flows without selector/tag/class fallbacks |
| Stable capture policy | Partial | Exact fresh-snapshot DOM/font/image readiness plus live DOM/layout and fetch/XHR-completion quiet-window evaluation is wired through an authenticated five-second fail-closed desktop gate before native capture. Managed pages enter bounded freeze/private-geometry/restore transactions; region/progressive/changed targets revalidate live state, and explicit full-page capture adds one 30-second exact-token transaction with bounded planning, per-tile settle/private-mask refresh, fixed/sticky rejection, native viewport acquisition, redaction-before-stitch and restoration-before-persistence. Strongly classified loopback Vite/Next.js/webpack HMR signals are now produced by managed-page instrumentation and feed the existing daemon-owned 300 ms HMR settle gate without retaining raw HMR payloads or socket tokens |
| Progressive capture regions | Implemented | Fresh authenticated `PageSnapshot` + stable `ElementRef` resolution drives bounded `element → component → section → viewport` targeting, with exact-level fail-closed selection, shared settle/freeze/native-capture/restore/redact authority and provenance-bearing receipts. React/Svelte/Vue ownership can enrich component targeting; changed-region scheduling and responsive transactions reuse the same auditable native acquisition boundary rather than adding platform-specific element screenshot APIs |
| Pixel visual diff | Implemented | Redacted native viewport/region pixels feed bounded pixel/tile diff and deterministic changed-region scheduling with the 96 MiB / 32-entry private baseline cache. Baselines advance only after evidence succeeds, and Trusted Fix/Verify now consumes affected-region visual evidence through the end-to-end capture → changed-region evidence → deterministic verification loop |
| Semantic snapshot model | Implemented | Managed WebViews emit bounded deep DOM/ARIA semantic trees with states, geometry, computed-style packets, visibility/occlusion and deltas; an authenticated fresh-snapshot endpoint projects a newly completed snapshot action into bounded `PageSnapshot` form. This is not claimed to be the native OS accessibility tree |
| Stable element refs | Implemented | Injected observer fingerprints + live bridge/action paths; progressive capture resolves exact stable refs from newly completed semantic snapshots |
| State diff | Implemented | Live semantic added/removed/changed refs plus layout deltas are transported through the observer path |
| Layout intelligence | Implemented | Live semantic/layout evidence is projected into a bounded computed-layout model covering flex/grid/container state, viewport/container overflow, clipping/occlusion evidence, bounded overlap and fixed/sticky collision cases, spacing rhythm and local alignment families. Invalid/missing viewport evidence fails closed instead of becoming a synthetic application defect |
| Responsive intelligence | Implemented | Canonical four-preset capture/contact-sheet execution is joined by bounded adaptive probing and binary refinement on the exact managed preview, with fixed-state/fixed-height truth fencing, probe caps, restore-before-persistence and observed-transition semantics that do not pretend to be CSS media-query source breakpoints. Transactional synthetic content/locale stress adds bounded expansion profiles, fresh semantic/layout evidence, new-overflow/collision detection and exact restoration; synthetic expansion is not claimed as real translation |
| Source mapping hints | Implemented | Explicit source hints, coordinate-independent component ownership, bounded Source Map v3 runtime resolution and React/Svelte/Vue exact-element ownership are connected through project-root-contained fail-closed authority. CSS declaration/cascade tracing can raise same-origin stylesheet hints to verified project files and exact declaration line/column only when a unique direct-CSS match or exact project-owned Source Map segment proves it. Remote/traversal/symlink/ambiguous/unsupported mappings remain unresolved rather than fabricated |
| Framework awareness | Partial | Bounded live ownership is connected for React (host-fiber authority plus bounded React 19 stack fallback), Svelte 5 compiler metadata and Vue compiler/runtime metadata, with explicit source hints retaining higher precedence and privacy-safe no-props/state retention. Additional framework-specific ownership beyond these landed paths remains optional breadth rather than a Wave 5 blocker |
| Console analysis | Partial | Warning/error/exception metadata is captured, securely drained and retained; richer action/route correlation remains |
| Network analysis | Partial | fetch/XHR completion metadata and failures are captured without response bodies and securely drained; privacy-safe aggregate in-flight accounting participates in capture settling; canonical action → request → UI-response correlation is live; and exact-session loopback fault authority now executes bounded fetch/XHR fail, delay and empty-body status mocks under finite leases/hit budgets with authenticated install/get/clear, explicit injected-fault observation metadata, compensation on invalid install acknowledgement, hit exhaustion/expiry/clear recovery and real non-loopback pass-through proof. Loopback-only strongly classified HMR timeline integration and bounded exact-session performance-lite packets are now live; Network analysis remains Partial because broader diagnosis/source attribution remains. Arbitrary internet interception, TLS MITM, response/body/header fixtures and generic WebSocket inspection/interception are not claimed |
| Accessibility analysis | Partial | Deterministic accessible-name/image/hit-target checks exist plus DOM/ARIA semantics; axe/native AX integration and journey verification remain |
| Performance analysis | Partial | Long-task/layout-shift observation now feeds one canonical bounded performance-lite packet in live analysis and authenticated exact-session `/performance-lite` control output. Full-window count/total/max remain truthful while only the deterministic longest-duration sample is retained under an 8-sample default / 16-sample hard cap; malformed measurements and arbitrary observer payload fields are excluded. CPU profiling, flamegraphs, heap/allocation profiling, Core Web Vitals completeness and broader attribution remain unclaimed |
| Interaction flows | Partial | Flow graph/replay primitives and deterministic page actions exist; discovery/replay verification is not yet complete |
| Design grammar | Partial | spacing/radius/type/control inference primitives exist; live project extraction and regression baselines remain |
| Observation bus | Implemented | Bounded observer history plus authenticated native drain |
| Diagnostics fusion | Partial | Deterministic/heuristic/subjective issue assembly exists; more live visual/source evidence still needs to feed it |
| Reports | Partial | JSON/Markdown/HTML renderers exist; complete CI/headless report production is pending |
| Artifact storage | Implemented | Bounded deduplicating local store primitives are used by the native visual coordinator with a 256 MiB visual budget. Private mask redaction and target/diff processing occur before the artifact store is reached. Progressive semantic resolution operates on metadata only; changed-region baselines are separate bounded in-process redacted RGBA cache entries rather than persisted raw baseline screenshots; capture receipts expose IDs/metadata rather than artifact paths |
| Engine tier escalation | Partial | Static/lightweight/native WebView/Chromium policy exists; on-demand Tier-3 execution is not yet the default verified visual path |
| Token budgeting | Partial | Compact/deep/minimal serialization budget primitives exist; active-perception budgeting and token-aware visual packet selection across progressive/changed-region tools remain |
| Local permission/security model | Implemented | Bearer token, loopback control plane, caller/session ownership checks, navigation guard and redaction policy; fresh progressive snapshots are authenticated/session-scoped and accept only the newly enqueued matching snapshot result. Native capture is dashboard-only, resolves only exact session-owned managed surfaces, serializes only captures belonging to the same session, fails closed on settle timeout, anchors fresh-snapshot presence to daemon evaluation time rather than the page clock, revalidates the live WebView route immediately before acquisition, keeps freeze/restore/private-selector plus full-page absolute-scroll/tile-probe controls off the generic public action queue, strips selector/private page payload from stored freeze results, restores exact visual state before processing pixels, rejects progressive route/viewport drift and changed/region viewport drift after restore, redacts private pixels before baselining/cropping, never silently widens missing component/section requests to viewport, keeps changed-region baselines bounded and redacted-only, advances those baselines only after selected evidence succeeds, and discards pixels if restoration, target validation or complete private-region redaction is not proven; full-page internal results never become public Interaction evidence and final full-page evidence contains bounded provenance only |
| MCP control plane | Partial | stdio MCP exposes live sessions/observer/actions plus synchronous `page.snapshot` and `page.inspect`; native visual/source/a11y/flow surfaces remain incomplete |
| AI Critic / Point-and-Ask | Partial | Direct point-select transport and stable-selection authority are implemented and feed Ask AI/Fix/Verify. The broader capability remains Partial because optional model-provider execution and later Wave 7 visual-critic/design-grammar reasoning are not complete |
| Secure observer drain | Implemented | Managed preview/workspace WebViews drain bounded observer events through caller/session-validated Tauri commands into the authenticated local control plane |
| Direct click/type agent actions | Implemented | Stable-ref click/type/key/scroll/focus/snapshot actions execute inside the managed WebView through the bounded queue/result bridge; internal visual freeze/restore/private-selector actions use a separate authenticated path and cannot be enqueued through the generic action endpoint |
| Native screenshot adapter | Implemented | Real WebView2/WKWebView/WebKitGTK **viewport** adapters are connected to the production capture path with bounded artifact persistence, fresh stable-settle gating, per-session freeze/private geometry, exact-token restore, live target validation and in-memory redaction. Explicit regions, progressive semantic targets, changed-region scheduling and guarded full-page stitching all reuse that one auditable viewport acquisition primitive; no platform-specific element/component/section/full-page screenshot backend was added. Dedicated hosted GUI gates prove all three platform backends with real rendered pixels through production-shared snapshot helpers. Linux runs GTK/WebKitGTK under Xvfb; macOS runs NSWindow + WKWebView on the real AppKit main thread; Windows creates a Win32 parent + installed WebView2 controller, navigates a deterministic `127.0.0.1` HTTP fixture with exact URI/NavigationId correlation, verifies DOM/CSS/geometry, calls the shared `CapturePreview` path, fully decodes the PNG and validates the known center pixel |
| Guarded full-page screenshot | Implemented | Explicit `capture_full_page` uses one per-session gate and one non-renewed 30-second freeze token; pure planning is bounded to 32 tiles / 50,000 CSS px / 128 MiB RGBA / 32,768 output-pixel height. Internal exact-token absolute scroll + fresh tile probes refresh private masks and reject visible fixed/sticky content before the existing native viewport copy. Every tile is redacted before decode/stitch, original scroll and visual state are restored before finalization, final persistence/evidence remains under the absolute deadline, and only one final artifact plus strict `visual-full-page` evidence is retained. No Chromium fallback, DOM/canvas reconstruction, infinite-scroll chasing, horizontal stitching, fixed/sticky normalization or intermediate tile persistence is claimed |
| Full replay / rich state timeline UI | Partial | Observation/action primitives exist; complete deterministic replay and desktop timeline are pending |
| Full DevTools replacement | Explicitly out of scope for v1 | Product spec says not to build it |
| Cloud accounts / collaborative remote browser | Explicitly out of scope for v1 | Local-first runtime by design |

## Completion rule

A capability is not called complete merely because a crate, panel, model or planner exists. `Implemented` requires a live execution path plus verification evidence. Pure algorithms that are valuable but not yet connected to the product remain `Partial` until their end-to-end path is real.
