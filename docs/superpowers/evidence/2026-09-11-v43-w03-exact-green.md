# V4.3 W03 exact GREEN evidence

This evidence note records the bounded W03 RED→GREEN chain for PR #113. It is intentionally source-neutral and exists to preserve exact-SHA verification provenance after the one-shot implementation runner committed as `github-actions[bot]`.

- Semantic RED boundary: the long-lived Windows real-provider gate reached W03 and failed because the shipping Windows UIA provider did not implement the virtualized-item query/realization path.
- Compile-diagnostic candidate: `a14c3881f542619a21e748ba9227a74a1a944253` captured the generated-tree compiler failure. The actual compile root cause was the worker layering boundary: virtualized methods existed on the inner worker while the public `subscription::platform::WindowsUiaWorker` wrapper lacked the delegates used by `virtualized_item.rs`.
- Corrected one-shot candidate: `fba25f8bc7e7c2d0d0602542f71b2291aaa02af5` added the narrow subscription-wrapper bridge. On that exact tree, Windows `cargo check`, WPF edge-seed build, and the exact ignored real-provider test `windows_real_provider_w03::w03_virtualized_item_requires_realize_then_fresh_observation` all passed before production code was committed.
- Production implementation commit: `33f7ce31653fe89df66a7d1c5098a76c3d539799` (`feat(v43): add bounded UIA virtualized-item realization primitive`). The one-shot patch scripts/workflow were removed by that commit.

This note does **not** claim PR #113, W05, or V4.3 complete. The production implementation remains subject to fresh long-lived CI, Windows UIA, and Windows real-provider verification on the current PR head.
