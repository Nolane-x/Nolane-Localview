# D1 RED checkpoint

Task 1 RED head: `e5ee285e16e2183a246136d027beae559250fa67`.

The only implementation-facing change at this checkpoint is `crates/sessions/tests/session_lineage_contract.rs`. Production lineage APIs are intentionally absent so CI can prove the contract is RED for the intended reason before implementation begins.
