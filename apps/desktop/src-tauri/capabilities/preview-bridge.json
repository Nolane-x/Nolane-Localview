{
  "$schema": "../gen/schemas/remote-schema.json",
  "identifier": "preview-bridge-capability",
  "description": "Least-privilege bridge for LocalView-managed loopback preview and workspace WebViews. IPC is granted only to URLPattern forms accepted by Tauri; IPv6 loopback remains unprivileged until the upstream URLPattern parser accepts the literal safely. No core, filesystem, shell, arbitrary network, or dashboard permissions are granted.",
  "webviews": ["preview-*", "workspace-*"],
  "local": false,
  "remote": {
    "urls": [
      "http://localhost:*/*",
      "http://127.0.0.1:*/*"
    ]
  },
  "permissions": ["previewbridge"]
}
