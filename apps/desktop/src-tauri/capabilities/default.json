{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Bundled LocalView dashboard capability. Preview windows do not inherit this capability.",
  "windows": ["main"],
  "permissions": ["core:default", "maincommands"]
}
