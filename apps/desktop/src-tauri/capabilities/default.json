{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "LocalView dashboard core capability. Preview windows intentionally receive no command capability.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
