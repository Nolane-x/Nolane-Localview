name: V4.3 Task 8 SetValue Safe Preflight RED

on:
  push:
    branches: [feat/v43-windows-set-value-payload-authority]

permissions:
  contents: read

jobs:
  safe-preflight-red:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Run SetValue safe preflight RED contract
        run: cargo test -p localview-windows-observe-runtime --test set_value_safe_preflight_contract -- --nocapture
