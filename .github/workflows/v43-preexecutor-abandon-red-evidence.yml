name: V43 pre-executor abandonment RED evidence

on:
  push:
    branches:
      - feat/v43-consequential-verified-execution-coordinator

jobs:
  red:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - run: cargo test -p localview-windows-observe-runtime --test execution_coordinator_behavior stale_canonical_authority_before_executor_releases_live_execution_grant -- --nocapture
