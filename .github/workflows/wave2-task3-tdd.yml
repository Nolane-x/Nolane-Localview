name: Wave 2 Task 3 red-green gate

on:
  push:
    branches:
      - feat/guarded-full-page-stitching-impl
    paths:
      - .github/workflows/wave2-task3-tdd.yml

permissions:
  contents: write

jobs:
  task3:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/guarded-full-page-stitching-impl
      - name: Install dependencies
        run: sudo apt-get update && sudo apt-get install -y libwebkit2gtk-4.1-dev build-essential pkg-config
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - name: Verify RED
        run: |
          set +e
          cargo test -p localview-desktop --test full_page_stitch_instrumentation_contract -- --nocapture 2>&1 | tee /tmp/task3-red.log
          status=${PIPESTATUS[0]}
          set -e
          test "$status" -ne 0
          grep -q "instrumentation_owns_bounded_full_page_freeze_scroll_and_probe_authority" /tmp/task3-red.log
          grep -q "FAILED" /tmp/task3-red.log
      - name: Apply Task 3 implementation
        run: python scripts/wave2_task3_patch.py
      - name: Verify GREEN
        run: |
          cargo fmt --all -- --check
          cargo test -p localview-desktop --test full_page_stitch_instrumentation_contract -- --nocapture
          cargo test -p localview-instrumentation --all-targets
      - name: Commit implementation
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add crates/instrumentation/src/lib.rs apps/desktop/src-tauri/src/lib.rs
          git diff --cached --check
          git commit -m "feat(capture): add token-bound full-page page executor"
          git push origin HEAD:feat/guarded-full-page-stitching-impl
