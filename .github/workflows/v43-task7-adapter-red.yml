name: V4.3 Task 7 adapter RED

on:
  push:
    branches: [feat/v43-windows-set-value-payload-authority]

permissions:
  contents: read

jobs:
  adapter-red:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Compile production SetValue adapter contract
        run: cargo test -p localview-windows-observe-runtime --test set_value_runtime_provider_adapter_contract --no-run
