name: Task 8 Write Token Probe

on:
  push:
    branches:
      - feat/v43-windows-set-value-payload-authority

permissions:
  contents: write

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - run: echo task8-write-token-probe-ok
