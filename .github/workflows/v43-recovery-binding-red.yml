name: V43 recovery binding RED
on:
  push:
jobs:
  red:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - run: cargo test -p localview-live-bridge --test v43_recovery_binding_inventory -- --nocapture
